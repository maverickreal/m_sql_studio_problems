id: "01a08dde-83b5-754f-8fab-c7c1f19c9140"
slug: "authors-with-no-published-work"
title: "Authors with no published work"
description: |
  Author names who have zero published articles.
difficulty: easy
mode: read
category: content
datasets:
  - content
sampleInput:
  - "authors(id, name, email, joined_date)"
  - "articles(id, title, author_id, category, published_date, word_count, status)"
  - "comments(id, article_id, commenter_name, body, posted_date)"
  - "tags(id, name)"
  - "article_tags(article_id, tag_id)"
  - "likes(id, article_id, user_name, liked_date)"
sampleOutput: "gold-diff"
initSql: |
  -- Dataset: content
solutionSql: |
  SELECT a.name FROM authors a LEFT JOIN articles ar ON ar.author_id = a.id AND ar.status = 'published' GROUP BY a.name HAVING COUNT(ar.id) = 0 ORDER BY a.name
validationSql: |
  SELECT 1 AS ok
orderMatters: true
origin: first-party
author: "maverickreal"
license: "CC-BY-4.0"
schema_version: 1
